# dev reembed runtime

- PID launch/profile layer rebuilt.
- Win32 embed pipeline now uses SetParent + style sync + Qt container.
- Live check: SovAuto autodetected local 1C, launched it, embedded child HWNDs, no external top-level 1C window remained.
- Profile runtime refresh now updates title/base metadata as 1C flow changes.
